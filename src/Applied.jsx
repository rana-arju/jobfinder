import React from 'react';
import Header from './components/header/Header';

const Applied = () => {
    return (
        <div>
            <Header />
            applied
        </div>
    );
};

export default Applied;